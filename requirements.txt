# Install via conda (see README or setup instructions):
#   conda install pytorch -c pytorch
#   conda install -c conda-forge scikit-learn matplotlib numpy

# Install via pip (not on conda):
#   pip install tiktoken datasets

torch>=2.0.0
datasets>=2.14.0
tiktoken>=0.5.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
numpy>=1.24.0
